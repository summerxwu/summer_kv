#![allow(unused_variables)]
#![allow(dead_code)]
pub mod blocks;
mod sstable;
mod util;

mod iterator;
pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
